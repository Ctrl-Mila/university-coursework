## Practice 1 – Basic Concurrency

This practice introduces the concept of concurrency using multiple robots
executing independent tasks in parallel within the CMRE (R-Info) environment.

The selected exercise demonstrates:
- Concurrent execution of four robots
- Independent behaviors without synchronization
- Modular design using reusable processes
